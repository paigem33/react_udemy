import React  from 'react'

const SaladIngredients = (props) => (
    <>
        <div>Navigation here</div>
        <main>
            {props.children}
        </main>
    </>
)

export default SaladIngredients;